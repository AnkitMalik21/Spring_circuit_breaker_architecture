server:
  port: 8082

spring:
  application:
    name: HotelService
  datasource:
    url: jdbc:postgresql://localhost:5432/hotel
    username: postgres
    password: Malik@1
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
