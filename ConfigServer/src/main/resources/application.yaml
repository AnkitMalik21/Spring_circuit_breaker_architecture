server:
  port: 8085
spring:
  application:
    name: ConfigServer

  cloud:
    config:
      server:
        git:
          uri: https://github.com/AnkitMalik21/Spring_circuit_breaker_architecture.git
          clone-on-start: true
          search-paths: ConfigRepo
